/*
============================================================
CS213 Data Structures
Lab 01: Pointers, References, and Dynamic Memory
============================================================

ชื่อ-นามสกุล: วิรากร ธนบัตร
รหัสนักศึกษา: 6809617415

คำชี้แจง
1. แบบฝึกปฏิบัตินี้เป็นงานรายบุคคล
2. แก้ไขเฉพาะบริเวณที่ระบุ TODO หรือ STUDENT CODE เท่านั้น
3. ห้ามแก้ไขส่วนที่ระบุ DO NOT MODIFY
4. ห้ามเปลี่ยนชื่อฟังก์ชันหรือ parameters ที่กำหนดให้
5. โปรแกรมเริ่มต้นสามารถ compile ได้ นักศึกษาควร compile/run
   หลังจากทำแต่ละส่วนเพื่อทดสอบงานของตนเอง
6. เมื่อเสร็จแล้วบันทึกชื่อไฟล์เป็น Lab01_StudentID.cpp

คะแนนรวม 10 คะแนน
============================================================
*/

#include <iostream>
using namespace std;

// ============================================================
// PART A: Pointer Trace [2 คะแนน]
// ============================================================
/*
พิจารณาการทำงานของ partA() แล้วตอบคำถามต่อไปนี้

A1. โปรแกรมแสดงผลอะไร
TODO: 15 25

A2. เมื่อจบการทำงาน p ชี้ไปยังตัวแปรใด
TODO: y

A3. เมื่อจบการทำงาน *p มีค่าเท่าใด
TODO: 25

A4. อธิบายสั้น ๆ ว่าเหตุใดค่า x และ y
จึงเปลี่ยนเป็นค่าที่เห็นในผลลัพธ์
TODO:
ก่อนบรรทัด `p = &y`:
    pointer p ชี้ไปยัง x
    จากนั้นใช้ *p เพื่อ dereference เขียนค่าทันไปยังค่าของ x ผ่าน pointer p
    ดังนั้น variable x จึงมีค่า = 15 ตาม `*p = 15`

ตั้งแต่บรรทัด `p = &y`:
    pointer p ถูกชี้ไปยัง y แทนที่ x ผ่าน `p = &y`
    และใช้การ dereference แบบเดียวกับที่ทำกับ x
    ซึ่งจากบรรทัด `*p = *p + 5` หมายถึง `y = y + 5`
    ดังนั้น variable y จึงมีค่าเท่ากับ 25

*** ห้ามแก้ไขโค้ดใน partA() ***
*/
void partA() {
  // ===== DO NOT MODIFY =====
  int x = 10;
  int y = 20;
  int *p = &x;

  *p = 15;
  p = &y;
  *p = *p + 5;

  cout << x << " " << y << endl;
  // ===== END DO NOT MODIFY =====
}

// ============================================================
// PART B: Pointer and Reference [1.5 คะแนน]
// ============================================================
/*
พิจารณาการทำงานของ partB() แล้วตอบคำถามต่อไปนี้

B1. p และ r อ้างถึงข้อมูลเดียวกันหรือไม่ เพราะเหตุใด
TODO: ใช่ ทั้ง p และ r ชี้ไปยังจุดเดียวกันคือ x แต่แค่มีวิธีการที่ต่างกัน
    p ชี้ไปยัง x ผ่าน pointer คือการที่ p เก็บ address ของ x ไว้
    r เก็บ reference ของ x แทนที่จะเก็บ address

B2. คำสั่ง *p = 20; ส่งผลต่อตัวแปรใด
TODO: x

B3. คำสั่ง r = 30; ส่งผลต่อตัวแปรใด
TODO: x

B4. อธิบายความแตกต่างระหว่าง pointer p
และ reference r จากตัวอย่างนี้
TODO: ตามคำตอบใน B1 ขยายความคือ
    pointer p เก็บ address ซึ่งต้อง dereference หากต้องการข้อมูลที่เก็บอยู่ในตัวแปร p
    ส่วน reference r เก็บ reference ตรงๆของ x จึงไม่จำเป็นต้อง dereference
หรือใช้การดำเนินการใดๆในการเข้าถึงข้อมูลในตัวแปร r

*** ห้ามแก้ไขโค้ดใน partB() ***
*/
void partB() {
  // ===== DO NOT MODIFY =====
  int x = 10;
  int *p = &x;
  int &r = x;

  *p = 20;
  r = 30;

  cout << x << " " << *p << " " << r << endl;
  // ===== END DO NOT MODIFY =====
}

// ============================================================
// PART C: Fix the Pointer [1.5 คะแนน]
// ============================================================
/*
pointer p ยังไม่ได้ชี้ไปยังตำแหน่งหน่วยความจำที่ถูกต้อง

จงเพิ่มคำสั่งเฉพาะในบริเวณ STUDENT CODE เพื่อให้
p ชี้ไปยัง x และทำให้โปรแกรมแสดงผล 10 โดยใช้ pointer อย่างถูกต้อง

ห้าม:
- แก้ไขโค้ดนอก STUDENT CODE
- สร้างตัวแปรเพิ่มเติม
- ใช้ new หรือ delete ในข้อนี้
*/
void partC() {
  int x = 10;
  int *p = nullptr;

  // ===== STUDENT CODE =====
  // TODO: ทำให้ p ชี้ไปยัง x
  p = &x;
  // ===== END STUDENT CODE =====

  // ===== DO NOT MODIFY =====
  if (p != nullptr)
    cout << *p << endl;
  else
    cout << "[Part C not completed]" << endl;
  // ===== END DO NOT MODIFY =====
}

// ============================================================
// PART D: Dynamic Memory [2 คะแนน]
// ============================================================
/*
เขียนคำสั่งเฉพาะในบริเวณ STUDENT CODE ให้ทำงานตามลำดับดังนี้

1. จองหน่วยความจำสำหรับ int จำนวน 1 ค่า โดยใช้ new
2. กำหนดค่า 25 ลงในหน่วยความจำผ่าน pointer p
3. แสดงค่าดังกล่าวผ่าน pointer p
4. คืนหน่วยความจำด้วย delete
5. หลังคืนหน่วยความจำ ให้กำหนด p = nullptr

Expected output:
25

ห้าม:
- แก้ไข int* p = nullptr;
- สร้าง pointer เพิ่ม
- สร้างตัวแปร int เพิ่ม
- ใช้ cout << 25 โดยตรง
- แก้ไขโค้ดนอก STUDENT CODE
*/
void partD() {
  int *p = nullptr;

  // ===== STUDENT CODE =====
  // TODO: allocate -> store 25 -> print -> delete -> reset
  p = new int;
  *p = 25;
  cout << *p << endl;
  delete p;
  p = nullptr;
  // ===== END STUDENT CODE =====
}

// ============================================================
// PART E: Analyze Values Using Pointers [2 คะแนน]
// ============================================================
/*
จงเติมฟังก์ชัน analyze() ให้รับจำนวนเต็มสองค่าผ่าน a และ b แล้ว

1. เก็บ "ค่าที่มากกว่า" ลงในตำแหน่งที่ larger ชี้อยู่
2. เก็บ "ผลต่างระหว่างค่ามากและค่าน้อย" ลงในตำแหน่งที่
   difference ชี้อยู่ โดยผลต่างต้องไม่ติดลบ

ตัวอย่างที่ 1
Input:
10 25
Output:
Larger = 25
Difference = 15

ตัวอย่างที่ 2
Input:
30 12
Output:
Larger = 30
Difference = 18

ห้าม:
- แก้ไขชื่อฟังก์ชันหรือ parameters
- ใช้ abs(), max(), min()
- ใช้ global variable
- แก้ไข main()
*/
void analyze(int *a, int *b, int *larger, int *difference) {
  *larger = 0;
  *difference = 0;

  // ===== STUDENT CODE =====
  // TODO: อ่านค่าผ่าน a และ b แล้วบันทึกผลผ่าน larger และ difference
  *larger = (*a > *b) ? *a : *b;
  *difference = *larger - ((*a < *b) ? *a : *b);
  // ===== END STUDENT CODE =====
}

// ============================================================
// PART F: Concept Check [1 คะแนน]
// ============================================================
/*
F1. กำหนดให้
    int x = 10;
    int* p = &x;
อธิบายว่า p และ *p มีความหมายแตกต่างกันอย่างไร
TODO: p เก็บ address ของ x, *p คือการ dereference เพื่อให้ได้ค่าที่เก็บอยู่ใน x

F2. เพราะเหตุใดหน่วยความจำที่จองด้วย new
จึงควรคืนด้วย delete
TODO: การจองค่าด้วย new คือการ allocate memory บน heap memory
delete คือการคืนที่ให้ program เอาที่ไปใช้งานอื่นๆต่อ หากไม่ delete
ข้อมูลหน่อยความจำตรงนั้นจะไม่สามารถถูกใช้โดย process อื่นๆได้
*/

// ============================================================
// MAIN PROGRAM - DO NOT MODIFY
// ============================================================
int main() {
  cout << "=== Part A ===" << endl;
  partA();

  cout << "\n=== Part B ===" << endl;
  partB();

  cout << "\n=== Part C ===" << endl;
  partC();

  cout << "\n=== Part D ===" << endl;
  partD();

  cout << "\n=== Part E ===" << endl;
  int x, y;
  cout << "Enter two integers: ";
  cin >> x >> y;

  int larger = 0;
  int difference = 0;
  analyze(&x, &y, &larger, &difference);

  cout << "Larger = " << larger << endl;
  cout << "Difference = " << difference << endl;
  return 0;
}
